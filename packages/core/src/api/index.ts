// API client — fetch-first abstraction (ADR-005)
export { createApiClient } from './client';
export type { ApiClient, ApiClientConfig, ApiResponse, RequestOptions } from './types';
export { ApiError } from './types';
